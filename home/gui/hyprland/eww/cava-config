[general]
# Match waybar cava settings exactly
framerate = 120
bars = 10
autosens = 1
sensitivity = 1.5
lower_cutoff_freq = 50
higher_cutoff_freq = 15000

[input]
# Use pipewire pulse compatibility with monitor source
method = pulse
source = @DEFAULT_MONITOR@
sample_rate = 44100
sample_bits = 16

[output]
method = raw
raw_target = /tmp/eww-cava.fifo
data_format = ascii
ascii_max_range = 8
# Proper delimiters for space-separated output
bar_delimiter = 32
frame_delimiter = 10
bit_format = 8bit

[color]
# Colors not needed for raw output
gradient = 0

[smoothing]
# Use proper noise reduction like in waybar
noise_reduction = 0.77

[eq]
# Reduced EQ settings to prevent over-amplification
1 = 0.8
2 = 0.8
3 = 0.8
4 = 0.8
5 = 0.8
