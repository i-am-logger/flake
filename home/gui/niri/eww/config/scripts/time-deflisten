#!/usr/bin/env bash

# Smooth time deflisten script - updates every second precisely

# Output initial time immediately
date '+%A | %r | %m-%d-%Y'

# Continuous time updates with precise timing
while true; do
    # Sleep for exactly 1 second
    sleep 1

    # Output the time
    date '+%A | %r | %m-%d-%Y'
done
